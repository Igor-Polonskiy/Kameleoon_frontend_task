# A/B Test Statistics Visualization

Проект представляет собой интерактивный линейный график для визуализации статистики A/B тестов, построенный на **React** с **TypeScript**. Позволяет анализировать динамику конверсии по вариациям с различными настройками отображения.

🚀 Посмотреть демо на GitHub Pages: [https://igor-polonskiy.github.io/Kameleoon_frontend_task/](https://igor-polonskiy.github.io/Kameleoon_frontend_task/)

---

## Выбранная библиотека для визуализации

Для построения графиков была использована библиотека **[Recharts](https://recharts.org/)**.  

---

## Реализованные функции

### Основные функции
- 📈 **Отображение графика конверсии** с расчётом `conversionRate = (conversions / visits) * 100`.
- 🎯 **Интерактивный tooltip** с отображением данных для всех доступных вариаций.
- 🔘 **Выбор вариаций** — пользователь может включать или отключать конкретные линии, при этом минимум одна вариация всегда остаётся активной.
- 📏 **Автоматическое масштабирование осей** при переключении вариаций или изменении периода агрегации.
- 📱 **Адаптивный интерфейс**, оптимизированный для ширины экрана от 671px и выше.

### Дополнительно реализованные функции (улучшения)

- 🗓️ **Изменение таймфрейма**  
  Пользователь может переключать отображение данных между **дневным** и **недельным** режимами. При выборе недельного режима данные агрегируются с пересчётом среднего процента конверсии.

- 🌗 **Тёмная тема**  
  Реализовано полноценное переключение темы с использованием контекста. Все элементы (графики, селекты, кнопки) корректно адаптируются под выбранную тему.

- ✒️ **Три вида линий**  
  Пользователь может выбрать стиль отображения графика:
  - Line (обычная линия)
  - Smooth (сглаженная линия)
  - Area (залитая область)

- 🔍 **Зум графика**  
  Реализован zoom-in / zoom-out с изменением горизонтального масштаба графика от 100% до 200%. Управление зумом вынесено в панель управления. Также зум работает при прокрутке колеса мыши!

- 🖼️ **Экспорт графика в PNG**  
  В верхней панели есть кнопка для сохранения текущего состояния графика в виде изображения.

---

## Локальная установка и запуск

Для запуска проекта на вашей локальной машине потребуется **Node.js** (версия 14 или выше рекомендуется).

1. Клонируйте репозиторий:

```bash
git clone https://github.com/Igor-Polonskiy/Kameleoon_frontend_task.git
cd Kameleoon_frontend_task
Установите зависимости:

bash
Копировать код
npm install
Запустите проект в режиме разработки:

bash
Копировать код
npm run dev
Приложение будет доступно по адресу: http://localhost:5173 (или на другом порту, если 5173 занят).

Доступные скрипты
npm run dev – Запуск dev-сервера с Vite.

npm run build – Сборка проекта для продакшена.

npm run preview – Предпросмотр собранной версии проекта.

npm run deploy – Деплой проекта на GitHub Pages.

Технологический стек
React 18 + TypeScript

Recharts – библиотека для визуализации

CSS Modules – для стилизации компонентов

Vite – сборщик проекта

GitHub Pages – хостинг